{
  "name": "onsse",
  "private": true,
  "version": "0.0.0",
  "description": "Onsse – bouclier temporel pour salariés et indépendants",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.33.3"
}
